[general]
name = MenuItemType
valuetype = generate

[list]
Command = cmd, command,
CheckBox = check, checkbox,
SingleChoice = single, siglechoice, radio, radiobox
SubMenu = menu, submenu, sub-menu
Line = line