# Input

AppCUI allows input from:
* Keyboard
* Mouse

The input is received via the `OnKeyPressed` and `OnMouseEvent` traits, and they are usually used when designing a [Custom Control](../chapter-3/custom_controls.md).
